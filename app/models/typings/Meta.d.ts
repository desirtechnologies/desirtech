export type MetaProps = {
    title?: string,
    pageTitle?: string,
    description?: string,
    canonical?: string,
    twitter?: {
        handle?: string,
        site?: string,
        cardType: string
    }
}